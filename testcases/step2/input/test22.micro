PROGRAM sqrt
BEGIN

	STRING dummy := "abcde";  --This is dummy to satisfy the grammar

     INT i, j, iprime;
     
	FUNCTION VOID main()
	BEGIN
		i := 0;
		iprime := 0;
		j := 0;
		result := 0;
		
		DO
		     i := i + 1;
		     IF (i > 69)
		          CONTINUE;
		     ENDIF
		     iprime := iprime + 1;
		     DO
		          IF (j > 49)
		               BREAK;
		          ENDIF
		          j := j + 1;
	               result := result + 1;
		     WHILE (j < 100);
		WHILE (i < 100);

		WRITE (result);
		WRITE (j);
		WRITE (i);
	END
END
