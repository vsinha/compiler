PROGRAM test
BEGIN
    FUNCTION VOID main()
    BEGIN
    INT a, b;
    a := 2;

    IF (a = 1)
      IF (a > 0)
        b := a + 4;
      ELSE
        b:= 3;
      ENDIF
    ELSE
      b := 2;
    ENDIF

    END
END
